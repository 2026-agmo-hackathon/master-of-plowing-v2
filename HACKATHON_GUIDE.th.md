# SeamOS Hackathon 2026: The Master of Plowing — คู่มือผู้เข้าแข่งขัน

> เอกสารนี้เป็นคู่มือปฏิบัติสำหรับผู้เข้าแข่งขัน SeamOS Hackathon 2026
> สำหรับภูมิหลังของการแข่งขันและเกณฑ์การประเมินผล กรุณาดูเอกสารแผนการแข่งขันแยกต่างหาก เอกสารนี้ครอบคลุมขั้นตอนการปฏิบัติตั้งแต่ **ตั้งค่า repository → เขียน RDDF → นำไปใช้ในแอป**

---

## 1. ภาพรวมการแข่งขัน

- **ภารกิจ**: ควบคุม tractor (รถแทรกเตอร์) เสมือนจริงเพื่อไถแปลง **"Alpha"** ให้ได้**เวลาสั้นที่สุด + พื้นที่มากที่สุด**
- **ข้อจำกัดหลัก**: ความเร็วสูงสุด **7 km/h** (ค่าที่สูงกว่าจะถูกปฏิเสธ ไม่ใช่ clamping แบบเงียบ)
- **การคิดคะแนน**: ใช้ไฟล์ CSV ที่บันทึกด้วย REC เพียงไฟล์เดียวคิดคะแนน **เต็ม 100**
  - หกรายการ — **Coverage 45 · Duplicate 17 · Time 16 · Retread 9 · Outside 7 · Outside Time 6**
  - ค่าความสำเร็จ (0–1) ของแต่ละรายการจะถูก**ยกกำลังสาม**ก่อนคูณด้วยคะแนนเต็มของรายการนั้น คะแนนจึงเพิ่มเร็วเฉพาะเมื่อใกล้เต็มเท่านั้น
  - ตัวอย่าง: ครอบคลุม 50% ได้ **5.6** จาก 45 คะแนน Coverage (0.5³ = 12.5%)
  - **ไม่มี hard cutoff** เช่น 95% — แม้ครอบคลุมต่ำก็ยังคิดคะแนนให้
- **ผลเป็นโมฆะ (INVALID)**: ขับเอง กดคีย์บอร์ด หรือเปลี่ยนอุปกรณ์ระหว่างวิ่ง = **ไม่คิดคะแนนเลย**

> **สาระสำคัญ**: สิ่งที่ผู้เข้าแข่งขันต้องเขียนและส่งคือ **RDDF (ไฟล์นิยามเส้นทาง)**
> อัลกอริทึมควบคุม tractor (รถแทรกเตอร์) มีให้พร้อมใน skeleton ที่จัดเตรียมไว้แล้ว ผู้เข้าแข่งขันจะแสดง**เส้นทางที่เหมาะสมที่สุดสำหรับการไถแปลง**ผ่าน RDDF

---

## 2. สิ่งที่ต้องเตรียม

| รายการ | หมายเหตุ |
|--------|----------|
| **SeamOS IDE (FeatureDesigner)** | ใช้เวอร์ชันที่ทีมงานแจกจ่าย |
| **Claude Code** | ติดตั้งเวอร์ชันล่าสุดจากเว็บไซต์ทางการ |
| **SeamOS Everywhere** | ใช้เวอร์ชันที่ทีมงานแจกจ่าย |
| **SeamOS World (emulator)** | สภาพแวดล้อมที่ใช้วิ่ง RDDF จริงและตรวจสอบผล ใช้ตัวติดตั้งในเครื่องหรือ `seamosworld.seamos.io` |


---

## 3. โครงสร้าง repository

```
master_of_plow/
├── com.bosch.fsp.master_of_plow/            # FSP (Feature Spec Project)
├── com.bosch.fsp.master_of_plow.gen/        # ผลลัพธ์ที่สร้างจาก FSP
├── com.bosch.fsp.master_of_plow.gen.tests/  # ผลลัพธ์เทสต์ที่ถูกสร้างขึ้น
├── master_of_plow_app/                      # แอปหลัก (C++ code, การตั้งค่า)
│   ├── src-gen/AppMain/tracking/            # โครงโค้ดการติดตามเส้นทาง (§10)
│   └── tests/                               # โมดูลเทสต์ในเครื่อง
├── master_of_plow_CPP_SDK/                  # SeamOS C++ SDK (จัดเตรียมไว้ให้)
├── customui-src/                            # ซอร์สของแดชบอร์ด (React + Vite)
├── rddf/                                    # พื้นที่ RDDF ที่ผู้เข้าแข่งขันเขียนและตรวจสอบ
├── docs/                                    # เอกสาร run orchestration และการตรวจสอบ FIF
├── distribution/                            # ผลลัพธ์สำหรับการแจกจ่าย
├── seamos-assets/                           # รูปภาพ marketplace และ asset อื่น ๆ
└── HACKATHON_GUIDE.md                       # เอกสารนี้ (แนบคำแปล en · de · th)
```

### 3.1 เปิดใน SeamOS IDE

1. เปิด SeamOS IDE
2. **File → Open Project...**
3. ระบุ path `master_of_plow/` ที่ clone มาแล้วเปิด

### 3.2 การตรวจสอบ build

ใช้ SeamOS IDE build โปรเจกต์ จากนั้นรันแอปและรันการทดสอบเพื่อยืนยันว่าสภาพแวดล้อมปกติ

#### 1. ทำการ build

คลิกปุ่ม **Build** บน toolbar ด้านบน จากนั้นคลิก **Build all** เพื่อ build โปรเจกต์ทั้งหมด
- SDK (SeamOS C++ SDK)
- App (แอปหลัก)
- Test (โมดูลทดสอบ local)

ทั้งสามโมดูลต้อง build สำเร็จทั้งหมด

#### 2. package, install และตรวจสอบ

รัน local tests แล้วใช้ SDK/Feature Designer ที่ผู้จัดให้มา (หรือ CI pipeline)
สร้าง FIF ติดตั้ง FIF ผ่าน workflow ปกติของ emulator จากนั้นให้ผู้ใช้เลือกและ
start feature การคัดลอก executable เข้า emulator โดยตรงไม่ใช่ build/validation
workflow ที่รองรับ ดู `docs/official-fif-validation-workflow.md`

---

## 4. RDDF คืออะไร?

**RDDF (Road Definition Data File)** คือ**ลำดับ waypoint** ที่ tractor (รถแทรกเตอร์) จะตาม
นี่คือผลลัพธ์หลักที่ผู้เข้าแข่งขันส่งในการแข่งขัน และนิยามสิ่งต่อไปนี้

- จะไปที่ไหน (ละติจูด/ลองจิจูด)
- จะไปเร็วแค่ไหน (ความเร็ว)
- เดินหน้า/ถอยหลัง (เครื่องหมายของความเร็ว)
- จะ**ลงไถ/ยกไถ**ที่จุดนั้น (implement flag)

### 4.1 รูปแบบไฟล์

**9 คอลัมน์** คั่นด้วย tab (`\t`) หนึ่งบรรทัดต่อหนึ่ง waypoint:

> parser รับ**รูปแบบ 7 คอลัมน์**ที่ตัด `lineNo` และ `index` ด้านหน้าออกได้ด้วย
> (`RddfParser.cpp`) แต่คู่มือนี้และไฟล์ตัวอย่างใช้ 9 คอลัมน์ ซึ่งเป็นแบบที่แนะนำ

```
lineNo  index  lat         lon          res1  res2  res3  speed  implementFlag
1       1      35.8000317  126.8807033  0.0   0.0   0.0   3.00   1
2       2      35.8000317  126.8807141  0.0   0.0   0.0   3.00   1
...
22      22     35.8001131  126.8808124  0.0   0.0   0.0   -1.00  0
23      23     35.8001040  126.8808124  0.0   0.0   0.0   -1.00  0
```

| คอลัมน์ | ความหมาย |
|---------|-----------|
| `lineNo` | หมายเลขบรรทัดในไฟล์ (เพิ่มขึ้นจาก 1) |
| `index` | ดัชนี waypoint (ปกติเหมือนกับ lineNo) |
| `lat` | ละติจูด (decimal degrees, WGS84) |
| `lon` | ลองจิจูด (decimal degrees, WGS84) |
| `res1`, `res2`, `res3` | ฟิลด์สำรอง — ใช้ `0.0` เสมอ |
| `speed` | ความเร็วเป้าหมาย (km/h). **ค่าลบ = ถอยหลัง** |
| `implementFlag` | `1` = ลงไถ (plowing on), `0` = ยกไถ (turn/idle) |

### 4.2 กฎเรื่องความเร็ว

- `0 < speed ≤ 7.0` — เดินหน้า; ค่าต่ำกว่า `2.05 km/h` จะโหลดพร้อมคำเตือน
- `speed = 0` — waypoint หยุดอย่างชัดเจน
- `-7.0 ≤ speed < 0` — ถอยหลัง ไม่มี penalty เฉพาะของการถอยหลัง แต่เวลาที่เสียไปกับการหยุดและเร่งใหม่ รวมถึงงานที่ซ้ำซ้อน จะทำให้เสียคะแนน (§7.4); ค่าสัมบูรณ์ต่ำกว่า `2.05 km/h` จะมีคำเตือน
- หากเครื่องหมายของความเร็วเปลี่ยนระหว่าง waypoint ที่อยู่ติดกัน ถือเป็น**จุดเปลี่ยนเกียร์** — ต้องจัดวางให้ heading (ทิศทางการเคลื่อนที่) กลับทิศด้วย

### 4.3 implementFlag

- เมื่อกำลังไถบนแถว: `1`
- เมื่อกำลังเลี้ยวบน headland (พื้นที่หัวแปลง) (ขอบ field): `0`
- **ช่วงถอยหลังต้องเป็น `0` เสมอ** — ไม่มีการไถขณะถอยหลัง
- แม้ขณะเลี้ยวบน headland ถ้าอุปกรณ์ผ่านพื้นที่ที่ทำงานไปแล้วก็นับเป็น `Retread` (§7.2)

### 4.4 การตรวจสอบอัตโนมัติ (Validation)

RDDF จะผ่านการตรวจสอบอัตโนมัติทันทีที่ได้รับจากคลาวด์ ข้อผิดพลาดด้านรูปแบบและการเกินเพดานความปลอดภัยจะ**ถูกปฏิเสธโดยไม่บันทึก** ส่วนข้อจำกัดทางกายภาพของการติดตามจะโหลดค่าเดิมพร้อมคำเตือน ลอจิกการตรวจสอบนิยามไว้ใน `RddfValidator.cpp`

| การตรวจ | เนื้อหา | หากฝ่าฝืน |
|---------|---------|-----------|
| **ไฟล์ว่าง** (Rule 4) | จำนวน waypoint เป็น 0 | ปฏิเสธ |
| **ความถูกต้องของค่า** | `lat` · `lon` · `speed` ต้องเป็นจำนวนจำกัด (ห้าม NaN หรือ inf) | ปฏิเสธพร้อมระบุหมายเลข waypoint |
| **implementFlag** | รับเฉพาะ `0` หรือ `1` | ปฏิเสธพร้อมระบุค่าจริงและหมายเลข waypoint |
| **เพดานความเร็ว** | `\|speed\| ≤ 7.0 km/h` | เกินเพดานให้ปฏิเสธ |
| **ขีดล่างความเร็ว** | ค่าที่ไม่เป็นศูนย์และต่ำกว่า `2.05 km/h` | โหลดค่าเดิมและเตือน |
| **ระยะห่าง waypoint** (Rule 3) | ระยะระหว่างจุดที่ติดกัน **ไม่น้อยกว่า 0.05 m และไม่เกิน 5.0 m** | ปฏิเสธพร้อมระบุคู่ waypoint และระยะจริง |
| **ความโค้งทางกายภาพ** (Rule 6) | วัดความโค้งจากการเปลี่ยน heading ของ 3 จุดต่อเนื่อง แล้วเทียบกับรัศมีเลี้ยวขั้นต่ำของยานพาหนะ (ราว **4.35 m**) | โหลดค่าเดิมและเตือนเรื่องขีดจำกัดการติดตาม |

> ค่าคงที่อยู่ใน `RddfValidator.hpp`: `MIN_WAYPOINT_SPACING_M = 0.05`,
> `MAX_WAYPOINT_SPACING_M = 5.0`, `MAX_MACHINE_SPEED_KMH = 7.0`,
> `MIN_MACHINE_SPEED_KMH = 2.05` ส่วนรัศมีเลี้ยวขั้นต่ำคำนวณจากค่าคงที่ของ tracker
> โดยตรงเป็น `WHEELBASE_M / tan(WHEEL_MAX_RAD)`
> การปฏิเสธจะ**หยุดที่จุดที่ผิดจุดแรก** จึงรายงานเหตุผลได้ครั้งละหนึ่งข้อเท่านั้น

#### พฤติกรรมเมื่อถูกปฏิเสธ

- simulator จะแสดง **dialog "RDDF Validation Error"** ทันที พร้อมเหตุผลที่ถูกปฏิเสธ
- RDDF ที่ถูกปฏิเสธจะไม่เข้ารายการ pending และถูกลบอัตโนมัติ — ต้อง**แก้ไขและอัปโหลดใหม่**
- หาก dialog ไม่ปรากฏและเข้ารายการ pending ได้ปกติ ถือว่าผ่านการตรวจสอบ

#### Start pose เป็น safety gate ของ session

การตรวจสอบไฟล์แบบ static จะไม่สร้าง StartPoint ขึ้นเอง เนื่องจาก loader ไม่มี
ตำแหน่ง map spawn แบบ live ที่เชื่อถือได้ ก่อนอนุญาตให้เคลื่อนที่ simulator/session
ต้องเปรียบเทียบ pose ล่าสุดของรถกับ waypoint 0

> หมายเหตุ: loader เก็บค่าความเร็วที่ส่งมาตามเดิม ค่านอกเหนือจากศูนย์หรือ
> ปฏิเสธเฉพาะค่าสัมบูรณ์เกิน `7.0 km/h` ค่าที่ไม่เป็นศูนย์และต่ำกว่า
> `2.05 km/h` จะโหลดตามเดิม และการใช้ creep floor จะแสดงใน telemetry

### 4.5 ข้อมูลแผนที่ (Field Maps)

การแข่งขันมี **4 แผนที่** โดย M1–M3 เปิดเผย ส่วน **M4 เป็น hidden** และสัญญาณจะถูกบล็อกจนกว่าทีมงานจะเปิดเผย polygon (ขอบเขต field) ของแต่ละแผนที่นิยามด้วยพิกัด WGS84 `lat, lon` และ RDDF ของผู้เข้าแข่งขันต้องครอบคลุมพื้นที่ภายใน polygon ให้ได้มากที่สุด

| แผนที่ | ชื่อ | Origin (lat, lon) | พื้นที่ | จำนวนจุดยอด | การเปิดเผย |
|--------|------|-------------------|---------|-------------|------------|
| **M1** | Open Fields | 35.8000, 126.8800 | 1,500 m² | 4 | เปิดเผย |
| **M2** | Sloped Acres | 34.6800, 126.9100 | 1,948 m² | 37 | เปิดเผย |
| **M3** | Patchwork Plots | 35.4200, 127.3900 | 1,355 m² | 54 | เปิดเผย |
| **M4** | Crooked Bottoms | 35.2000, 127.4600 | 1,879 m² | 89 | **hidden** |

> polygon ของ M4 จะแจกจ่ายเมื่อทีมงานเปิดเผย ก่อนหน้านั้นสัญญาณของแผนที่นั้นถูกบล็อก
> จึงเตรียม RDDF ล่วงหน้าไม่ได้ ทั้ง 4 แผนที่รวมพื้นที่ 6,682 m²

> Origin คือจุดอ้างอิง GPS ที่สอดคล้องกับ (0, 0) ใน local ของ polygon (เพื่อการอ้างอิง) และพื้นที่คำนวณด้วยสูตร shoelace ในหน่วย m²

#### M1 · Open Fields (1,500 m², 4 จุดยอด)

![M1 Open Fields](docs/maps/map1_polygon.png)

แปลงรูปสี่เหลี่ยมเรียบง่ายที่สุด เหมาะสำหรับการตรวจสอบอัลกอริทึม boustrophedon อย่างรวดเร็ว

```
polygon = [
  (35.8002897, 126.8806012), (35.8004622, 126.8802035),
  (35.8001628, 126.8800000), (35.8000000, 126.8803803),
]
```

#### M2 · Sloped Acres (1,948 m², 37 จุดยอด)

![M2 Sloped Acres](docs/maps/map2_polygon.png)

รูปหลายเหลี่ยมซับซ้อนที่จำลองแปลงลาดชัน headland (พื้นที่หัวแปลง) ยาวทำให้การจัดการ U-turn ทำได้ยาก

```
polygon = [
  (34.6805868, 126.9102540), (34.6804636, 126.9100401),
  (34.6804514, 126.9100264), (34.6804341, 126.9100117),
  (34.6804101, 126.9100000), (34.6803773, 126.9100074),
  (34.6803447, 126.9100296), (34.6803099, 126.9100506),
  (34.6802487, 126.9100895), (34.6801798, 126.9101240),
  (34.6800194, 126.9101844), (34.6800025, 126.9101929),
  (34.6800000, 126.9102047), (34.6800011, 126.9102220),
  (34.6800076, 126.9102429), (34.6800240, 126.9102651),
  (34.6800455, 126.9102861), (34.6800750, 126.9103133),
  (34.6801236, 126.9103631), (34.6801328, 126.9103724),
  (34.6801435, 126.9103860), (34.6801583, 126.9104057),
  (34.6801838, 126.9104396), (34.6802037, 126.9104747),
  (34.6802139, 126.9104994), (34.6802247, 126.9105198),
  (34.6802354, 126.9105480), (34.6802425, 126.9105771),
  (34.6802543, 126.9106086), (34.6802578, 126.9106314),
  (34.6802619, 126.9106436), (34.6802696, 126.9106499),
  (34.6802767, 126.9106461), (34.6802850, 126.9106363),
  (34.6803441, 126.9105654), (34.6803610, 126.9105499),
  (34.6803819, 126.9105290),
]
```

#### M3 · Patchwork Plots (1,355 m², 54 จุดยอด)

![M3 Patchwork Plots](docs/maps/map3_polygon.png)

แปลงที่มีทั้งรูปเว้าและนูนผสมกัน boustrophedon อย่างง่ายจะเหลือพื้นที่ที่ยังไม่ได้ไถมาก จึงต้องใช้ polygon clipping และประมวลผลแยกพื้นที่ย่อย

```
polygon = [
  (35.4205363, 127.3902241), (35.4205148, 127.3901085),
  (35.4205020, 127.3900000), (35.4202816, 127.3900305),
  (35.4202305, 127.3900267), (35.4201433, 127.3900066),
  (35.4201374, 127.3900667), (35.4201845, 127.3900979),
  (35.4202004, 127.3901073), (35.4202145, 127.3901207),
  (35.4202350, 127.3901547), (35.4202510, 127.3901941),
  (35.4202633, 127.3902458), (35.4202652, 127.3902776),
  (35.4202611, 127.3903620), (35.4201999, 127.3903605),
  (35.4201579, 127.3903998), (35.4201428, 127.3904155),
  (35.4201145, 127.3904243), (35.4200712, 127.3904327),
  (35.4200707, 127.3904499), (35.4200667, 127.3904538),
  (35.4200640, 127.3904632), (35.4200598, 127.3904727),
  (35.4200525, 127.3904733), (35.4200484, 127.3904905),
  (35.4200105, 127.3904933), (35.4200014, 127.3905578),
  (35.4200000, 127.3905707), (35.4200178, 127.3905879),
  (35.4200319, 127.3906068), (35.4200430, 127.3906307),
  (35.4200766, 127.3906202), (35.4201037, 127.3906140),
  (35.4201269, 127.3906073), (35.4201511, 127.3905483),
  (35.4201598, 127.3905333), (35.4201770, 127.3905294),
  (35.4202127, 127.3905333), (35.4202578, 127.3905433),
  (35.4202830, 127.3904800), (35.4202971, 127.3904454),
  (35.4203168, 127.3904127), (35.4203341, 127.3903826),
  (35.4203501, 127.3903605), (35.4203743, 127.3903364),
  (35.4203994, 127.3903132), (35.4204189, 127.3902909),
  (35.4204427, 127.3902630), (35.4204637, 127.3902486),
  (35.4204810, 127.3902407), (35.4205012, 127.3902347),
  (35.4205213, 127.3902308), (35.4205394, 127.3902358),
]
```

---

## 5. ขั้นตอนการทำงานสำหรับเขียน RDDF

### 5.1 โครงสร้างไดเรกทอรี

```
master_of_plow/rddf/
├── 1.rddf                  # เส้นทางสำหรับ field 1
├── 2.rddf                  # เส้นทางสำหรับ field 2
├── 3.rddf                  # เส้นทางสำหรับ field 3
├── upload_rddf.sh          # script อัปโหลดขึ้นคลาวด์
├── how-to-upload-rddf.md   # บันทึกคำสั่งอัปโหลด
└── README.md               # สรุปข้อกำหนดไฟล์และพฤติกรรมการตรวจสอบ
```

### 5.2 เขียน RDDF โดยตรง

วิธีที่ง่ายที่สุดคือใช้ text editor เขียนข้อมูลคั่นด้วย tab โดยตรง

```
1	1	35.8000317	126.8807033	0.0	0.0	0.0	3.00	1
2	2	35.8000317	126.8807141	0.0	0.0	0.0	3.00	1
3	3	35.8000317	126.8807249	0.0	0.0	0.0	3.00	1
```

> **ข้อควรระวัง**: ตัวคั่นต้องเป็น **tab (`\t`)** เท่านั้น หากมีช่องว่าง (space) ปะปน parser จะปฏิเสธ

### 5.3 สร้าง RDDF ด้วยโค้ด

การเขียน waypoint หลายสิบถึงหลายร้อยจุดด้วยมือเป็นเรื่องที่ทำได้ยาก โดยทั่วไปจะเขียน script ที่รับ**รูปร่างแปลง + พารามิเตอร์อัลกอริทึม** (ระยะห่างระหว่างแถว รัศมีเลี้ยว ฯลฯ) เป็น input แล้วสร้าง RDDF ออกมา ใช้ภาษาใดก็ได้ — คู่มือนี้ให้ตัวอย่างเป็น Python

#### ตัวอย่าง: boustrophedon แบบข้ามเลน + U-turn ครึ่งวงกลม

แบ่งแปลงเป็นแถวขนานด้วยระยะห่าง SWATH (ความกว้างไถ) และที่ปลายแถวใช้ **U-turn ครึ่งวงกลม
ไปข้างหน้า** เพื่อเข้าแถวถัดไป การไม่ใช้การถอยหลังช่วยประหยัดเวลาหยุดและเร่งใหม่

**การวิ่งไป-กลับเข้าแถวที่ติดกันโดยตรงนั้นทำไม่ได้** ครึ่งวงกลมนั้นมีรัศมี `SWATH / 2`
ซึ่งเมื่อ SWATH = 4 m จะได้ 2 m ต่ำกว่ารัศมีเลี้ยวขั้นต่ำของยานพาหนะที่ **4.35 m** มาก
พวงมาลัยจะหมุนจนสุดแต่รถก็ยังวิ่งเลยออกนอกโค้งอยู่ดี

จึงต้อง **ข้ามเลน** ในการเลี้ยว การเลี้ยวเข้าเลนที่ห่างออกไป `d` เลนจะได้รัศมี
`d × SWATH / 2` และค่านี้ต้องไม่ต่ำกว่า 4.35 m ในทุกการเลี้ยว

การไล่ **ครึ่งหน้าและครึ่งหลังของเลนสลับกัน** รับประกันเงื่อนไขนี้ได้ ถ้ามี 7 เลน
ลำดับการเข้าคือ `0, 4, 1, 5, 2, 6, 3` ช่วงห่างเป็น 4, 3, 4, 3, 4, 3 ดังนั้นการเลี้ยว
ที่แคบที่สุดก็ยังมีรัศมี `3 × 4 / 2 = 6 m`

```
เลน:     0    1    2    3    4    5    6
ลำดับ:   1    3    5    7    2    4    6
         └──── ครึ่งหน้า ────┘└─ ครึ่งหลัง ─┘

  ↑ เลน 0 ─────────╮
                   │  รัศมี = (จำนวนเลนที่ห่าง) × SWATH / 2  ≥ 4.35 m
  ↓ เลน 4 ─────────╯
```

ช่วงห่างที่น้อยที่สุดเท่ากับ `จำนวนเลนครึ่งหน้า − 1` รูปแบบนี้จึงต้องการให้แปลงกว้างกว่า
`SWATH × (2 × 4.35 / SWATH + 1)` หากแปลงแคบกว่านั้นจะใช้ U-turn ครึ่งวงกลมไม่ได้เลย
ต้องพิจารณา Ω-turn ที่ออกนอกขอบเขต (→ โดนหัก Outside) หรือการเลี้ยวสามจังหวะ
(→ เกิดช่วงถอยหลัง) แทน

#### โค้ดตัวอย่างขั้นต่ำ (`gen_rddf.py`)

```python
"""
gen_rddf.py - ตัวอย่างตัวสร้าง RDDF แบบ boustrophedon ข้ามเลน
ใช้เป็นจุดเริ่มต้น แล้วพัฒนาต่อเป็นอัลกอริทึมของคุณเอง
"""

import math

# --- จุดกำเนิดของแปลง (ล่างซ้าย) และการแปลงเป็น lat/lon ---
LAT0, LON0 = 35.8001, 126.8807
M_PER_DEG_LAT = 110540.0
M_PER_DEG_LON = 111320.0 * math.cos(math.radians(LAT0))

def to_latlon(x_m, y_m):
    return (LAT0 + y_m / M_PER_DEG_LAT,
            LON0 + x_m / M_PER_DEG_LON)

# --- ขีดจำกัดทางกายภาพของยานพาหนะ (ค่าเดียวกับ RddfValidator) ---
MIN_TURN_R  = 4.35     # WHEELBASE_M 2.05 / tan(WHEEL_MAX_RAD 0.44)
MAX_SPACING = 5.0      # ระยะห่างสูงสุดระหว่าง waypoint (เกินกว่านี้ไฟล์จะถูกปฏิเสธ)

# --- พารามิเตอร์ ---
FIELD_W = 30.0     # ความกว้างแปลง ตะวันออก-ตะวันตก (m)
FIELD_H = 50.0     # ความยาวแปลง เหนือ-ใต้ (m)
SWATH   = 4.0      # ระยะห่างเลน = ความกว้างไถ (m)
STEP    = 1.0      # ระยะห่าง waypoint (m) - ต้องไม่เกิน MAX_SPACING
V_LANE  = 3.0      # ความเร็วในเลน (km/h)
V_TURN  = 2.5      # ความเร็วขณะเลี้ยว (km/h) - ไม่ต่ำกว่าขีด creep 2.05
N_ARC   = 16       # จำนวนช่วงแบ่งของครึ่งวงกลม U-turn

assert STEP <= MAX_SPACING

n_lanes = int((FIELD_W - SWATH) // SWATH) + 1

# เลี้ยวเข้าเลนติดกันโดยตรงได้รัศมี SWATH/2 ซึ่งต่ำกว่าค่าต่ำสุด
# การสลับไปมาระหว่างครึ่งหน้าและครึ่งหลังทำให้ทุกช่วงห่างอย่างน้อย (half - 1) เลน
half = (n_lanes + 1) // 2
order = []
for i in range(half):
    order.append(i)
    if i + half < n_lanes:
        order.append(i + half)

# รัศมีเลี้ยวที่เล็กที่สุดที่ใช้ และระยะที่ครึ่งวงกลมยื่นเลยออกไป
MIN_R_USED = (half - 1) * SWATH / 2.0
HEADLAND   = half * SWATH / 2.0
assert MIN_R_USED >= MIN_TURN_R, (
    f"field too narrow: turn radius {MIN_R_USED:.2f} m < {MIN_TURN_R} m")

# เว้น headland ที่ปลายทั้งสองข้างเพื่อให้ครึ่งวงกลมอยู่ในแปลง
Y_LO, Y_HI = HEADLAND, FIELD_H - HEADLAND

waypoints = []  # (x, y, speed, implementFlag)
lane_x = lambda k: SWATH / 2.0 + k * SWATH

for n, k in enumerate(order):
    x = lane_x(k)
    going_up = (n % 2 == 0)

    # (1) เลนตรง - ลดอุปกรณ์ลง
    y0, y1 = (Y_LO, Y_HI) if going_up else (Y_HI, Y_LO)
    n_steps = max(2, int(abs(y1 - y0) / STEP) + 1)
    for i in range(n_steps):
        t = i / (n_steps - 1)
        waypoints.append((x, y0 + (y1 - y0) * t, V_LANE, 1))

    # (2) U-turn ครึ่งวงกลมเข้าสู่เลนถัดไป - ยกอุปกรณ์ขึ้น
    if n == len(order) - 1:
        break
    x_next = lane_x(order[n + 1])
    r = abs(x_next - x) / 2.0
    assert r >= MIN_TURN_R, f"turn radius {r:.2f} m < {MIN_TURN_R} m"
    cx, cy = (x + x_next) / 2.0, y1
    th0 = math.pi if x_next > x else 0.0
    th1 = 0.0 if x_next > x else math.pi
    sign = 1.0 if going_up else -1.0            # ปลายบนโป่งขึ้น ปลายล่างโป่งลง
    # จุดสุดท้าย (j = N_ARC) ซ้ำตำแหน่งกับจุดแรกของเลนถัดไป
    # พิกัดซ้ำกันสองครั้งทำให้ระยะห่างเป็น 0 และถูกปฏิเสธ
    for j in range(1, N_ARC):
        theta = th0 + (th1 - th0) * j / N_ARC
        waypoints.append((cx + r * math.cos(theta),
                          cy + sign * r * math.sin(theta),
                          V_TURN, 0))

# --- เขียนไฟล์ RDDF (คั่นด้วย tab, 9 คอลัมน์) ---
with open("alpha.rddf", "w") as f:
    for idx, (x, y, v, flag) in enumerate(waypoints, 1):
        lat, lon = to_latlon(x, y)
        f.write(f"{idx}\t{idx}\t{lat:.7f}\t{lon:.7f}\t0.0\t0.0\t0.0\t{v:.2f}\t{flag}\n")

print(f"{len(waypoints)} waypoints, {n_lanes} lanes, "
      f"min turn R {MIN_R_USED:.1f} m, headland {HEADLAND:.1f} m")
```

#### จุดที่ควรพัฒนาต่อด้วยอัลกอริทึมของตนเอง

script ข้างต้นครอบคลุมเฉพาะ**แปลงรูปสี่เหลี่ยม + แถวสม่ำเสมอ** เท่านั้น ในการแข่งขันจริงต้องแทนที่รายการต่อไปนี้ด้วยอัลกอริทึมของตนเอง

- **การประมวลผล polygon ขอบเขตแปลง** — ตัดความยาวแถวให้ตรงกับรูปร่างแปลงที่ไม่ใช่สี่เหลี่ยม
- **ไถ headland ให้เต็ม** — ไถแถบบน-ล่างที่ตัวอย่างเว้นไว้ด้วยรอบแยกตอนท้าย ถ้าปล่อยไว้ Coverage จะหายไปเท่านั้น
- **เลี้ยวให้จบภายในแปลง** — ครึ่งวงกลมที่ล้ำขอบเขตจะเสียคะแนน **Outside** ตามพื้นที่ และ **Outside Time** ตามเวลา (§7.2)
- **ลดการทำงานซ้ำ** — ผ่านเซลล์ที่เสร็จแล้วอีกครั้งนับเป็น **Duplicate** ส่วนล้อที่ทับพื้นที่ที่ทำแล้วนับเป็น **Retread** (§7.2)
- **การปรับแต่งระยะห่างระหว่างแถว / รัศมีเลี้ยว** — ลดระยะห่างแถวจะทำให้ช่วงเปลี่ยนแถวสั้นลง ตราบใดที่ทุกการเลี้ยวยังมีรัศมีไม่ต่ำกว่า **4.35 m**
- **จุดเริ่มต้น/สิ้นสุด** — ออกแบบเส้นทางเข้าจากจุด spawn ของแผนที่ไปยังแถวแรก
- **การลดพื้นที่ที่ยังไม่ได้ไถ** — จัดการพื้นที่เล็กๆ ที่ยังไม่ได้ไถบริเวณมุมและขอบแปลงแยกต่างหาก

### 5.4 Checklist การตรวจสอบ

ก่อนอัปโหลด RDDF ให้ตรวจสอบสิ่งต่อไปนี้

- [ ] ทุกบรรทัดมี 9 คอลัมน์และ**คั่นด้วย tab** อย่างสม่ำเสมอหรือไม่
- [ ] `lineNo` เพิ่มขึ้นจาก 1 โดยไม่มีช่องว่างหรือไม่
- [ ] start pose ของ map/session ตรงกับ waypoint 0 ก่อนอนุญาตให้เคลื่อนที่หรือไม่ (§4.4)
- [ ] ทุก `speed` มีค่าสัมบูรณ์ **ไม่เกิน 7.0 km/h** หรือไม่ (ต่ำกว่า `2.05 km/h` จะมีคำเตือน)
- [ ] ระยะห่างระหว่าง waypoint ที่ติดกันอยู่ในช่วง **0.05 m ~ 5.0 m** หรือไม่ — ถ้าออกนอกช่วงนี้ไฟล์จะถูกปฏิเสธ (§4.4 กฎข้อ 3)
- [ ] ช่วงเส้นตรงวางจุดห่างกัน **ไม่เกิน 5 m** หรือไม่ — เส้นตรงยาว ๆ ที่มีแค่จุดเริ่มต้นและจุดสิ้นสุดจะถูกปฏิเสธ
- [ ] ช่วงโค้งถี่พอหรือไม่ — ยิ่งความโค้งมาก ยิ่งต้องแบ่งละเอียดมากขึ้น
- [ ] เมื่อเปลี่ยนจากถอยหลังเป็นเดินหน้า **heading กลับทิศ** หรือไม่ (ไม่สามารถหมุนอยู่กับที่ได้)
- [ ] `implementFlag` ของทุกช่วงถอยหลังเป็น `0` หรือไม่
- [ ] รัศมีการเลี้ยวทุกจุดไม่ต่ำกว่า **4.35 m** หรือไม่ — แคบกว่านั้นแทรกเตอร์เลี้ยวไม่ได้ทางกายภาพ (§4.4)
- [ ] เส้นทาง U-turn ไม่เลยออกนอกขอบเขตแปลงหรือไม่ (โดนหัก Outside · Outside Time, §7.2)

---

## 6. นำ RDDF ไปใช้ในแอป

### 6.1 อัปโหลดขึ้นคลาวด์

การประเมินผลในการแข่งขันทำงานบน**RDDF ที่อัปโหลดขึ้นคลาวด์** ให้อัปโหลดโดยใช้ไฟล์ environment (Postman environment JSON) ที่ทีมงานออกให้ ร่วมกับ `FEU_ID` และ `FEATURE_ID`

> `--env` เป็นออปชันที่จำเป็น ไฟล์ environment มีคีย์ `tokenUrl`, `baseUrl`, `cp_client_id`,
> `cp_client_secret`, `feature_id`, `feu_id` ส่วน `--feature-id` และ `--feu-id` เป็นออปชันเสริม
> ที่ใช้เขียนทับค่าในไฟล์ ต้องติดตั้ง `jq` และ `curl` ไว้ด้วย

```bash
cd master_of_plow/rddf

# ให้สิทธิ์การรัน (ครั้งแรกครั้งเดียว)
chmod +x ./upload_rddf.sh

# อัปโหลด
./upload_rddf.sh \
  --env ./participant-env.json \
  -f ./1.rddf \
  --feu-id <YOUR_FEU_ID> \
  --feature-id <YOUR_FEATURE_ID>
```

ตัวอย่าง output เมื่อสำเร็จ:

```
Uploaded ./1.rddf (HTTP 200).
```

#### รูปแบบพารามิเตอร์

```
FEU_ID ตัวอย่าง      : abcdefgh-abcd-abcd-1234-abcd1234abcd
FEATURE_ID ตัวอย่าง  : 10234dev
```

#### ข้อผิดพลาดที่พบบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|-------|------------------|
| `jq is required` | `sudo apt install jq` หรือ `brew install jq` |
| `Status: 401` | FEU_ID / FEATURE_ID พิมพ์ผิดหรือหมดอายุ — ติดต่อทีมงาน |
| `Status: 4xx` (ไฟล์ถูกปฏิเสธ) | รูปแบบ RDDF ผิดพลาด — ตรวจสอบ checklist §5.4 อีกครั้ง |
| `Status: 5xx` | เซิร์ฟเวอร์ขัดข้องชั่วคราว — รอสักครู่แล้วลองใหม่ หากยังคงเกิดขึ้นติดต่อทีมงาน |

### 6.2 ยืนยันหลังอัปโหลด

หลังอัปโหลด แอปจะรับ RDDF ผ่าน `CloudDownloadListener` และ parse ด้วย `RddfParser` ตรวจสอบใน simulator ว่า tractor (รถแทรกเตอร์) วิ่งตามเส้นทางที่ตั้งใจไว้หรือไม่

---

## 7. สรุปกฎการแข่งขัน

### 7.1 เงื่อนไขภารกิจ

| หัวข้อ | ค่า |
|--------|-----|
| จุดเริ่มต้น | **จุด spawn ของแผนที่** จะได้สิทธิ์ขับก็ต่อเมื่อ waypoint 0 ของ RDDF ตรงกับจุดนั้น (§4.4) |
| แปลงเป้าหมาย | **M1 · M2 · M3** เปิดเผย ส่วน **M4** เป็น hidden — สัญญาณถูกบล็อกจนกว่าทีมงานจะเปิดเผย (§4.5) |
| เกณฑ์เป้าหมาย | **ไม่มี hard cutoff** — แม้ครอบคลุมต่ำก็ยังคิดคะแนนให้ |
| ความเร็วสูงสุด | **7 km/h** |
| อุปกรณ์ต่อพ่วง | **หนึ่งชิ้นต่อหนึ่งการวิ่ง** — เปลี่ยนระหว่างบันทึกจะทำให้การวิ่งนั้นเป็นโมฆะ |

### 7.2 การคิดคะแนน (เต็ม 100)

ใช้ **CSV ไฟล์เดียว** ที่บันทึกด้วย REC คิดคะแนน พื้นที่ของแต่ละแปลงไม่เท่ากัน
จึงต้องแปลงทุกค่าเป็น **สัดส่วน (0–1)** ก่อนคิดคะแนน

```
cov  = min(1, total_worked_m2 / work_area_m2)   ไถไปแล้วกี่ % ของแปลงที่ต้องไถ
dup  = duplicate_m2 / work_area_m2              ไถซ้ำโดยเปล่าประโยชน์กี่ % ของพื้นที่แปลง
out  = outside_m2   / work_area_m2              ไถหลุดออกนอกขอบเขตกี่ % ของพื้นที่แปลง
ret  = retread_m2   / total_worked_m2           พื้นที่ที่ตัวเองไถไว้ ถูกทับค้างอยู่กี่ %
outt = เวลาที่อยู่นอกขอบเขต / elapsed_s          ใช้เวลานอกแปลงกี่ % ของเวลาทั้งหมด
```

แต่ละตัวชี้วัดจะถูกแปลงเป็น **ค่าความสำเร็จ (0–1)** แล้ว **ยกกำลังสาม** ก่อนคูณคะแนนเต็ม

```
คะแนนรายการ = คะแนนเต็ม × ค่าความสำเร็จ³

SCORE = 45·cov³ + 17·dup_a³ + 16·time_a³ + 7·out_a³ + 6·outt_a³ + 9·ret_a³
```

| รายการ | ตัวชี้วัด | เกณฑ์เต็ม | เกณฑ์ 0 คะแนน | คะแนนเต็ม |
|--------|-----------|-----------|---------------|-----------|
| Coverage | `cov` | 100% | 0% | **45** |
| Duplicate | `dup` | ≤ 5% | ≥ 25% | **17** |
| Time | `v_eff` | ≥ 3.5 km/h | ≤ 0.5 km/h | **16** |
| Retread | `ret` | ≤ 5% | ≥ 20% | **9** |
| Outside | `out` | 0% | ≥ 10% | **7** |
| Outside Time | `outt` | 0% | ≥ 10% | **6** |

**การยกกำลังสามคือตัวชี้ขาด** คะแนนจะเพิ่มเร็วเฉพาะเมื่อค่าความสำเร็จใกล้เกณฑ์เต็มเท่านั้น

| ค่าความสำเร็จ | 0.5 | 0.7 | 0.8 | 0.9 | 0.95 | 1.0 |
|---------------|-----|-----|-----|-----|------|-----|
| สัดส่วนคะแนนที่ได้ | 13% | 34% | 51% | 73% | 86% | 100% |

ครอบคลุม 62% ต่อให้รายการอื่นเกือบเต็มหมด คะแนนรวมก็ยังไม่ถึง 60
**การไถแปลงให้เต็มจนจบสำคัญกว่าสิ่งอื่นใด**

#### Time ไม่ใช่วินาที แต่เป็นความเร็วทำงานที่มีประสิทธิผล

เวลาเป็นวินาทีล้วนขึ้นกับขนาดแปลงและความกว้างอุปกรณ์ จึงเทียบกันไม่ได้ แทนที่จะทำเช่นนั้น
เราหา **ระยะทางน้อยที่สุดที่ต้องวิ่งเพื่อไถแปลงนี้ให้ทั่ว (`D_ideal`) แล้วหารด้วยเวลาที่ใช้จริง**
เพื่อแปลงเป็นความเร็ว

```
R       = รัศมีเลี้ยวขั้นต่ำของแทรกเตอร์ (compact 3.2 / medium 4.0 / large 4.9 m)
W       = ความกว้างทำงานของอุปกรณ์ (m) — จากบรรทัด `# implement` ใน CSV
S       = ด้านสั้นของสี่เหลี่ยมล้อมเล็กสุดของ polygon แผนที่ (m)
N       = ceil(S / W)                              จำนวนรอบวิ่ง

D_ideal = work_area_m2 / W + (N − 1) × π × R       (m)
v_eff   = D_ideal / elapsed_s × 3.6                (km/h)
```

`elapsed_s` คือ `duration_ms / 1000` และเป็น **เวลา sim** ดังนั้นการวิ่งแบบเร่งความเร็ว
จึงไม่ทำให้บันทึกผลดีขึ้น

เวลาสำหรับคะแนนเต็มของแต่ละแผนที่ อิงแทรกเตอร์ใหญ่ (`R = 4.9`) กับไถใหญ่ (`W = 3.6`)

| แผนที่ | พื้นที่ (m²) | `D_ideal` (m) | เวลาคะแนนเต็ม |
|--------|--------------|---------------|----------------|
| M1 Open Fields | 1,500 | 570.5 | **587 s** (9น. 47ว.) |
| M2 Sloped Acres | 1,948 | 756.5 | **778 s** (12น. 58ว.) |
| M3 Patchwork Plots | 1,355 | 576.6 | **593 s** (9น. 53ว.) |
| M4 Crooked Bottoms | 1,879 | 737.5 | **759 s** (12น. 39ว.) |

ถ้าเปลี่ยนแทรกเตอร์หรืออุปกรณ์ ค่า `R` และ `W` จะเปลี่ยน และต้องคำนวณใหม่ทั้งหมด

### 7.3 การตัดสินเป็นโมฆะ (INVALID)

หากเข้าเงื่อนไขข้อใด**ข้อหนึ่ง** จะไม่คิดคะแนนและตัดออกจากการจัดอันดับ

| เงื่อนไข | วิธีตรวจจับ |
|----------|-------------|
| **ขับเอง** | มีแถวใน telemetry ที่ `manual == 1` **ตั้งแต่ 1 แถวขึ้นไป** |
| **กดคีย์บอร์ด** | **มี** บล็อก `event_t_ms,edge,key` อยู่ |
| **เปลี่ยนอุปกรณ์** | บรรทัด `# implement` **ตั้งแต่ 2 บรรทัดขึ้นไป** |
| **คิดคะแนนไม่ได้** | ไม่มีบรรทัด `# work_area_m2` หรือ `# map` |

ห้ามแตะคีย์บอร์ดขณะ REC ทำงาน แค่จับพวงมาลัยหรือแตะคันเร่ง/เบรกเพียงครั้งเดียว
การวิ่งทั้งรอบก็เป็นโมฆะ การเปลี่ยนแทรกเตอร์หรืออุปกรณ์ต้องทำให้เสร็จ**ก่อน**เริ่มบันทึก

### 7.4 กฎที่ถูกยกเลิกแล้ว

ส่วนที่ต่างจากคำแนะนำเดิม **สามข้อต่อไปนี้ไม่มีผลบังคับใช้แล้ว**

| กฎที่ยกเลิก | สถานะปัจจุบัน |
|-------------|----------------|
| **ถอยหลังในพื้นที่เพาะปลูกแล้วรีเซ็ตเป็นยังไม่ได้ไถ** | **ยกเลิก** พื้นที่ตกค้างจากการถอยหลังวัดได้เกือบเป็น 0 และความเสียหายจากการถอยหลังสะท้อนอยู่ใน Time กับ Duplicate อยู่แล้ว |
| **ออกนอกแปลงแล้วตัวจับเวลาเร็วขึ้น 10 เท่า** | **ไม่มีแล้ว** ถูกถอดออกจากแอปด้วย ปัจจุบันพื้นที่นอกขอบเขตคิดเป็น **Outside** และเวลานอกขอบเขตคิดเป็น **Outside Time** |
| **บวกค่าความคลาดเคลื่อนเฉลี่ยตอนจบ** | **ไม่ใช่รายการคิดคะแนน** ไม่มีสูตรอย่าง `finalTimeS = elapsedS + penaltyS` อยู่จริง |

ความคลาดเคลื่อนในการติดตามเส้นทางไม่ถูกคิดคะแนนโดยตรง แต่ถ้าคลาดเคลื่อนมากรถจะหลุดออกจากแถว
ทำให้ `cov` ลดลง และ `dup` กับ `ret` เพิ่มขึ้น จึง**เสียเปรียบทางอ้อมอย่างมาก**

### 7.5 วิธีการประเมินผล

- ให้วิ่งใน emulator โดย**เปิดการบันทึก REC** แล้วส่งไฟล์บันทึกการวิ่งที่เซฟไว้ขึ้น leaderboard
- ไฟล์ที่ใช้ส่งคือ **ไฟล์ผนึก (`.csv.enc`)** ซึ่งเปิดได้ด้วยคีย์ของทีมงานเท่านั้น ส่วนไฟล์ CSV แบบอ่านได้ที่ได้มาพร้อมกันมีไว้ให้วิเคราะห์เอง
- เวลาและพื้นที่ที่วัดใน local เป็นเพียงข้อมูลอ้างอิง **บันทึกทางการคือค่าใน leaderboard**
- คะแนนของแต่ละแผนที่รวมกันโดยใช้**ชื่อทีม**เป็น key

> คะแนนเต็ม ค่าที่ยอมรับได้ และวิธีส่งผลที่แน่นอน ให้ยึดตามประกาศของทีมงานเป็นหลัก
> ตัวเลขในเอกสารนี้คัดลอกมาจากกฎการคิดคะแนนของ leaderboard หากมีการเปลี่ยนแปลง
> ให้ยึดตามประกาศ

### 7.6 กฎการวิ่ง / การเชื่อมต่อ / ชื่อทีม (ต้องปฏิบัติอย่างเคร่งครัด)

> การฝ่าฝืนกฎด้านล่างอาจทำให้บันทึกการวิ่งนั้นเป็นโมฆะ และหากฝ่าฝืนซ้ำอาจเป็นเหตุให้ถูกตัดสิทธิ์

- **ไม่สามารถดำเนินการวิ่งสองรอบขึ้นไปพร้อมกันภายในแอป (simulator instance) เดียวกัน**
  - ต้องรอให้การวิ่งรอบก่อนหน้าสิ้นสุดสมบูรณ์ก่อนเริ่มรอบถัดไป
  - อย่าอัปโหลด RDDF ใหม่หรือรีสตาร์ท simulator ก่อนที่การวิ่งรอบก่อนหน้าจะเสร็จสิ้น
- **การวิ่งแบบขนานด้วยหลายแอป (instance) เป็นไปได้ แต่ห้ามวิ่งพร้อมกันด้วยชุด "ชื่อทีมเดียวกัน + แผนที่เดียวกัน"**
  - การวิ่งแผนที่ต่างกันพร้อมกันในแต่ละแอปเป็นไปได้ (เช่น แอป A คือ Map 1, แอป B คือ Map 2)
  - **ห้ามวิ่งแผนที่เดียวกันในสอง instance พร้อมกันด้วยชื่อทีมเดียวกัน** — การรวบรวมข้อมูลใน leaderboard จะเสียหายและบันทึกจะเป็นโมฆะ
- **ห้ามเชื่อมต่อหลาย browser พร้อมกัน**
  - **อย่าเชื่อมต่อพร้อมกันจากหลาย browser tab หรือหน้าต่างในแอป instance เดียวกัน**
  - การเชื่อมต่อหลายครั้งอาจทำให้เกิดข้อผิดพลาดในการรวบรวมข้อมูล leaderboard ความไม่สอดคล้องของพิกัด/ตัวจับเวลา และการสูญหายของผลลัพธ์
  - หากต้องการแชร์หน้าจอให้ใช้ฟีเจอร์ screen mirroring / screen sharing (ไม่ใช่ browser session แยก ✗)
- **ใช้ชื่อทีมเดียวกันในทุกการวิ่งทุกแผนที่**
  - คะแนนแต่ละแผนที่**รวมกันโดยใช้ชื่อทีมเป็น key** ดังนั้นหากต่างกันแม้แต่ตัวอักษรเดียวจะถูกมองว่าเป็นคนละทีมและคะแนนจะถูกแยกออก
  - ตัวอย่าง: `Team-A`, `team-A`, `Team A` (มีช่องว่าง) ถือเป็นคนละทีม
  - ยืนยันชื่อทีมก่อนการวิ่งรอบแรก และใช้**สตริงที่เหมือนกันทุกประการ**ในทุกแผนที่

---

## 8. ขั้นตอนการทำงานที่แนะนำ

```
[1] Clone repo & Import ใน IDE & ยืนยัน build ผ่าน
        ↓
[2] อัปโหลด rddf/1.rddf ตัวอย่างและสังเกตพฤติกรรมของ tractor ใน simulator
        ↓
[3] ออกแบบอัลกอริทึม — กำหนดความกว้างแถว การจัดการ headland รูปแบบการเลี้ยว
        ↓
[4] เขียน generator RDDF (เช่น gen_rddf.py) — implement อัลกอริทึมครอบคลุมพื้นที่ของตนเอง
        ↓
[5] ยืนยันว่า RDDF ที่สร้างขึ้นผ่าน checklist §5.4
        ↓
   ┌─→ [6] อัปโหลดขึ้นคลาวด์ด้วย upload_rddf.sh
   │        ↓
   │   [7] เปิด REC ใน emulator แล้ววิ่ง → ตรวจเวลาที่ใช้ / พื้นที่ที่ไถได้
   │        (ห้ามกดคีย์บอร์ดระหว่างบันทึก — แตะครั้งเดียวก็เป็นโมฆะ)
   │        ↓
   │   [8] ส่งไฟล์บันทึกการวิ่งที่เซฟไว้ขึ้น leaderboard → ยืนยันว่าบันทึกอัปเดตแล้ว
   │        ↓
   └── [9] ปรับแต่งอัลกอริทึม / พารามิเตอร์ → กลับไปที่ 4 หรือ 6

   * ทำซ้ำลูป [6]~[9] จนถึงกำหนดเวลา deadline และอัปเดตบันทึกใน leaderboard ทุกครั้ง
   * บันทึกสูงสุดของทีมใน leaderboard ณ กำหนดเวลา deadline (รวมทุกแผนที่)
     จะถูกนำมาประเมินผล
   * ถ้าติดอยู่ระหว่าง [2] กับ [3] ให้อ่าน §10 การรู้ว่าแทรกเตอร์ตอบสนอง
     ต่อเส้นทางอย่างไร ทำให้การออกแบบอัลกอริทึมในขั้น [3] ง่ายขึ้นมาก
```

---

## 9. FAQ

คำถามถูกจัดกลุ่มตามหัวข้อเพื่อให้ค้นหาได้สะดวก `(§N)` ในแต่ละคำตอบเป็นการอ้างอิงไปยังส่วนเนื้อหาที่เกี่ยวข้องในเอกสารนี้

- [9.1 การส่งและการประเมินผล](#91-การส่งและการประเมินผล)
- [9.2 การตรวจสอบและข้อผิดพลาด](#92-การตรวจสอบและข้อผิดพลาด)
- [9.3 การออกแบบเส้นทาง](#93-การออกแบบเส้นทาง)
- [9.4 กฎการดำเนินงาน](#94-กฎการดำเนินงาน)
- [9.5 สภาพแวดล้อมและเครื่องมือ](#95-สภาพแวดล้อมและเครื่องมือ)

---

### 9.1 การส่งและการประเมินผล

> **Q1.** ต้องส่งไฟล์ RDDF ไฟล์เดียวหรือส่งแยกตามแผนที่?

**อัปโหลดแยกตามแผนที่** การแข่งขันมี 4 แผนที่ โดย M1·M2·M3 เปิดเผยและ M4 เป็น hidden จากนั้นอัปโหลดไฟล์ RDDF ของแต่ละแผนที่แยกกัน (ระบุเป็นรายไฟล์ เช่น `upload_rddf.sh -f ./1.rddf` ไม่มีพารามิเตอร์ mapId) คะแนนแต่ละแผนที่รวมกันโดยใช้**ชื่อทีม**เป็น key (§4.5, §7.5)

---

> **Q2.** ต้องได้อัตราครอบคลุมพื้นที่ 100% ถึงจะชนะได้ไหม?

**ไม่มีเกณฑ์ที่ต้องผ่าน** แต่ Coverage มีคะแนนเต็มสูงที่สุดคือ 45 และค่าความสำเร็จถูก
**ยกกำลังสาม** ดังนั้นถ้าครอบคลุมต่ำ คะแนนจะพังลงอย่างรวดเร็ว

| ครอบคลุม | คะแนนที่ได้ (จาก 45) |
|---------:|---------------------:|
| 50% | 5.6 |
| 70% | 15.4 |
| 90% | 32.8 |
| 100% | 45.0 |

ยอมใช้เวลาเพิ่มอีกนิดเพื่อไถแปลงให้เต็มจนจบมักคุ้มกว่าเสมอ (§7.2)

---

> **Q3.** มีขั้นตอน "ส่งผลลัพธ์สุดท้าย" แยกต่างหากไหม?

ต้อง**ส่งไฟล์บันทึก REC (ไฟล์ผนึก `.csv.enc`) ขึ้น leaderboard** ทุกรอบการวิ่ง คะแนนจึงจะถูกนับ
การส่งนั่นเองคือการอัปเดตบันทึก สิ่งที่ทีมมีอยู่ใน leaderboard ณ กำหนดเวลา deadline คือสิ่งที่
ถูกนำมาประเมินผล จึงควรทำวน อัปโหลด → วิ่ง → ส่ง ไปจนถึงกำหนดเวลา (§7.5)

---

### 9.2 การตรวจสอบและข้อผิดพลาด

> **Q4.** อัปโหลด RDDF แล้วแต่ไม่มีอะไรเกิดขึ้นใน simulator

ให้ตรวจสอบสองความเป็นไปได้ต่อไปนี้

| สาเหตุ | การตรวจสอบ / การแก้ไข |
|--------|------------------------|
| **การตรวจสอบล้มเหลว** | ตรวจสอบว่า dialog "RDDF Validation Error" ปรากฏใน simulator หรือไม่ → แก้ไข RDDF ตามสาเหตุแล้วอัปโหลดใหม่ (§4.4) |
| **track เดิมยังค้างอยู่** | ตรวจสอบว่า RDDF ใหม่เข้าถึงรายการ pending แล้วหรือไม่ และขั้นตอน **apply** ไม่ถูกข้ามไป |

---

> **Q5.** เหตุใดตัวตรวจสอบไฟล์จึงไม่ตรวจ StartPoint ของแผนที่?

app loader ไม่มีตำแหน่ง live spawn ที่เชื่อถือได้ จึงให้ safety gate ของ
simulator/session ตรวจ start pose กับ waypoint 0 ก่อนอนุญาตให้เคลื่อนที่ (§4.4)

---

> **Q6.** สามารถเกิน 7 km/h เล็กน้อยได้ไหม?

**ไม่ได้** ค่าที่สูงกว่า 7.0 km/h จะถูกปฏิเสธ ไม่ถูก clamping และค่าที่ไม่ใช่
ค่าที่ไม่เป็นศูนย์แต่ต่ำกว่า 2.05 km/h จะยอมรับพร้อมคำเตือน เพราะเครื่องจักรอาจใช้
creep floor ระหว่างรันไทม์ (§4.2, §4.4)

---

### 9.3 การออกแบบเส้นทาง

> **Q7.** เส้นทางที่มี Overlap มากมีความได้เปรียบไหม?

**เสียเปรียบ และถูกหักคะแนนโดยตรงด้วย** การผ่านพื้นที่ที่เกินเกณฑ์เสร็จสมบูรณ์ไปแล้วซ้ำอีก
นับเป็น **Duplicate** ซึ่งมีคะแนนเต็ม 17

- **ไม่เกิน 5% ของพื้นที่แปลงได้คะแนนเต็ม** — การให้ความกว้างอุปกรณ์ซ้อนกันบ้างเป็นการใช้งานปกติเพื่อกันแถบที่ไม่ได้ไถ
- **เกิน 25% ได้ 0 คะแนน**
- ระหว่างนั้นค่าความสำเร็จถูกยกกำลังสาม แค่ 10% ก็เสียหายมากแล้ว

`Time` ก็ถูกหักไปด้วย จึงเสียเปรียบสองต่อ (§7.2)

---

> **Q8.** ใช้การถอยหลังขณะเลี้ยวได้ไหม?

**ได้ penalty เฉพาะของการถอยหลังถูกยกเลิกแล้ว** (§7.4)

แต่กรณีที่คุ้มค่านั้นมีน้อย

- ทุกครั้งที่สลับเดินหน้า↔ถอยหลังรถต้อง**หยุดสนิทแล้วเร่งใหม่** เสียเวลาไปเต็ม ๆ → `Time` แย่ลง
- ช่วงที่ถอยกลับไปคือพื้นที่ที่ผ่านมาแล้ว ทำให้ `Duplicate` และ `Retread` เพิ่มขึ้น
- ทุกช่วงถอยหลังต้องมี `implementFlag` เป็น `0` (§4.3)

ให้พิจารณาเป็นทางเลือกแทนการเลี้ยวสามจังหวะเฉพาะในแปลงที่แคบเกินกว่าจะใช้ U-turn ครึ่งวงกลมเท่านั้น

---

> **Q9.** การเลี้ยว U-turn ออกนอกแปลงมากๆ สะดวกกว่า แต่ penalty จะมากแค่ไหน?

**ไม่มีตัวจับเวลา 10 เท่าแล้ว** แต่จะถูกหักผ่านสองรายการแยกกัน

| รายการ | ตัวชี้วัด | เกณฑ์ 0 คะแนน | คะแนนเต็ม |
|--------|-----------|---------------|-----------|
| Outside | พื้นที่ที่ไถนอกขอบเขต ÷ พื้นที่แปลง | 10% | 7 |
| Outside Time | เวลาที่อยู่นอกขอบเขต ÷ เวลาทั้งหมด | 10% | 6 |

ทั้งสองรายการ**ได้คะแนนเต็มเฉพาะที่ 0%** เพราะถือเป็นข้อบกพร่องล้วน ๆ ที่ไม่มีอะไรบังคับให้เกิด
รวมกัน 13 คะแนนอาจดูไม่มาก แต่การเลี้ยวนอกแปลงก็กินเวลาด้วย `Time` จึงถูกหักตามไปอีก
การเลี้ยวให้จบภายในขอบเขตจึงได้เปรียบกว่า (§7.2)

---

> **Q10.** ความคลาดเคลื่อนในการติดตามเส้นทางมีผลต่อคะแนนมากแค่ไหน?

**ไม่ใช่รายการที่คิดคะแนนโดยตรง** ไม่มีวินาที penalty ที่ผูกกับค่าความคลาดเคลื่อนเอง (§7.4)

แต่ผลทางอ้อมนั้นใหญ่มาก เมื่อรถหลุดออกจากแถว เซลล์ที่ควรถูกไถก็ไม่ได้ไถ (`cov` ลดลง)
รถล้ำเข้าแถวข้าง ๆ แล้วไถพื้นที่ที่ทำไปแล้วซ้ำ (`dup` เพิ่ม) และล้อทับพื้นที่ที่ทำแล้ว
(`ret` เพิ่ม) รายการเหล่านี้รวมกัน 45 + 17 + 9 = 71 คะแนน

ค่าที่ดูได้จากแอปคือ **LTD (ค่าเบี่ยงเบนด้านข้าง)** บนแดชบอร์ด และคอลัมน์ `cte` ใน
`/tmp/pp_trace.csv` ถ้าค่านี้สะสมไปทางเดียวเรื่อย ๆ ในช่วงโค้ง แปลว่าช่วงนั้นเกินขีดจำกัด
การติดตามของยานพาหนะแล้ว

→ สามารถลดได้ด้วยการเพิ่ม**ความหนาแน่นของ waypoint ในช่วงโค้ง**และ**ลด heading jump** (§10.6)

---

### 9.4 กฎการดำเนินงาน

> **Q11.** สามารถวิ่งสองแผนที่พร้อมกันในอุปกรณ์เดียวกันได้ไหม?

**ได้ — แต่ห้ามใช้ "ชื่อทีมเดียวกัน + แผนที่เดียวกัน" พร้อมกัน**

- OK: วิ่ง M1 ในแอป A และ M2 ในแอป B พร้อมกัน
- NG: วิ่ง M1 พร้อมกันใน 2 instance ด้วยชื่อทีมเดียวกัน
- NG: เชื่อมต่อด้วยหลาย browser tab ใน instance เดียวกัน

(§7.6)

---

> **Q12.** สามารถเปลี่ยนชื่อทีมระหว่างการแข่งขันได้ไหม?

**ไม่แนะนำ** ชื่อทีมเป็น key สำหรับรวมคะแนนแต่ละแผนที่ ดังนั้น**หากต่างกันแม้แต่ตัวอักษรเดียว**จะถูกมองว่าเป็นคนละทีมและคะแนนจะถูกแยกออก ให้ยืนยันชื่อทีมก่อนการวิ่งรอบแรกและใช้สตริงที่เหมือนกันทุกประการในทุกแผนที่และทุกการวิ่ง (§7.6)

---

### 9.5 สภาพแวดล้อมและเครื่องมือ

> **Q13.** สามารถสร้างเครื่องมือเขียน RDDF generator ได้อย่างอิสระหรือไม่?

**ได้ — ไม่มีข้อจำกัดด้านเครื่องมือหรือภาษา** Python, MATLAB, อัลกอริทึมของตนเอง — ใช้ได้ทั้งหมด สิ่งที่ต้องการคือไฟล์ `.rddf` ที่ตรงตามรูปแบบในเอกสารนี้เป็นผลลัพธ์สุดท้าย (§4.1)

---

> **Q14.** สามารถแก้ไขโค้ด (แอป C++, tracker การติดตามเส้นทาง ฯลฯ) ได้ไหม?

**ได้** ผู้เข้าแข่งขันสามารถแก้ไข autonomy logic และ parameters ได้ การประเมิน
อย่างเป็นทางการใช้ FIF/app build ของผู้เข้าแข่งขันที่ได้รับอนุมัติร่วมกับ RDDF
ตามกฎปัจจุบันของผู้จัด การเปลี่ยนแปลงต้อง package/install ผ่าน workflow FIF
อย่างเป็นทางการ (§3.2); local build หรือ binary ที่คัดลอกโดยตรงไม่ได้รับสิทธิ์โดยอัตโนมัติ

อย่างไรก็ตาม การ**อ่าน**โค้ดการติดตามเส้นทางเป็นสิ่งที่แนะนำ การรู้ว่าแทรกเตอร์ติดตามเส้นทางแบบไหนได้ดี และเส้นทางแบบไหนทำให้ค่าความคลาดเคลื่อนโตขึ้น จะช่วยยกระดับคุณภาพการออกแบบ RDDF ได้โดยตรง §10 อธิบายโครงสร้างและวิธีอ่านไว้แล้ว

---

## 10. ขั้นสูง: ทำความเข้าใจโค้ดการติดตามเส้นทาง (ทางเลือก)

> tracker เป็นส่วนหนึ่งของ app ผู้เข้าแข่งขัน การเปลี่ยนแปลงจะใช้ในการประเมิน
> อย่างเป็นทางการได้เมื่อรวมอยู่ใน FIF ที่ได้รับอนุมัติตามกฎผู้จัด (§9.5 Q14)
> การอ่านโค้ดยังช่วยให้เข้าใจ geometry ของ RDDF ที่รถติดตามได้อย่างเสถียร

### 10.1 ตำแหน่งของโค้ด

การติดตามเส้นทางรวมอยู่ในที่เดียว: `master_of_plow_app/src-gen/AppMain/tracking/`

```
AppMain/tracking/
├── TrackerTypes.hpp      ข้อกำหนดระบบพิกัด/เครื่องหมาย + ชนิดข้อมูล  ← เริ่มที่นี่
├── IPathTracker.hpp      สัญญา (interface) ที่อัลกอริทึมการติดตามต้องทำตาม
├── PathTrackerBase.hpp   คลาสฐานที่เขียนส่วนซ้ำ ๆ ไว้ให้แล้ว
├── SpeedController.hpp   เกียร์/คันเร่ง/เบรก (ใช้ร่วมกันทุกอัลกอริทึม)
├── SteeringController.hpp ส่งคำสั่งพวงมาลัย
├── TrackerFactory.*      ชื่อ → การนำไปใช้
├── TrackerSwitch.*       จัดการการสลับ tracker ระหว่างรัน
├── TrackingLoop.*        ลูปควบคุม (แปลง GPS, ส่งคำสั่ง, telemetry)
├── SampleClock.hpp       ฐานเวลาของรอบการควบคุม
├── ControlTimeGate.hpp   เกตของรอบการควบคุม
├── GpsSampleStore.hpp    ที่เก็บตัวอย่างข้อมูล GPS
├── SignalFreshness.hpp   การตัดสินความสดของสัญญาณ
├── ClockLog.hpp / ClockTelemetry.hpp  การวินิจฉัยเรื่องเวลา
└── impl/
    ├── PurePursuitTracker.*  tracker เริ่มต้น (ตัวที่วิ่งจริงในการแข่งขัน)
    └── StanleyTracker.*      ตัวอย่างการนำไปใช้ตัวที่สอง
```

คอมเมนต์ทั้งหมดเขียนไว้**ทั้งภาษาเกาหลีและภาษาอังกฤษ** เริ่มจากด้านบนของ
`TrackerTypes.hpp` เพราะถ้าไม่รู้ข้อกำหนดระบบพิกัด ส่วนที่เหลือจะอ่านผิดความหมาย

### 10.2 ระบบพิกัดและเครื่องหมาย — สิ่งที่ต้องรู้ก่อน

| หัวข้อ | ข้อกำหนด |
|--------|----------|
| ตำแหน่ง | ENU หน่วยเมตร `(x, z)`, **+x = ทิศตะวันออก, +z = ทิศใต้** จุดกำเนิด = waypoint ที่ 0 ของ RDDF |
| ทิศทาง (heading) | `atan2(z, x)` เรเดียน 0 = หันไปทางตะวันออก, +π/2 = หันไปทางใต้ |
| การเลี้ยว (steering) | ค่าบวก = **เลี้ยวซ้าย** ระบบเป็นแบบมือซ้าย ดังนั้นการเลี้ยวซ้ายทำให้ heading *ลดลง* |
| ความเร็ว | ค่าใน waypoint เป็น km/h **มีเครื่องหมาย** (ลบ = ถอยหลัง); ค่าที่วัดได้เป็น m/s **ไม่มีเครื่องหมาย** |

ค่า heading แบบเข็มทิศจาก GPS (0 = ทิศเหนือ, ตามเข็มนาฬิกา) จะถูกลูปควบคุมแปลงเข้าสู่ระบบนี้ให้

### 10.3 หนึ่งรอบของการควบคุม

`TrackingLoop` ทำซ้ำลำดับนี้ทุก 100 ms (สั้นลงตามสัดส่วนเมื่อเพิ่มความเร็วการจำลอง)

```
isReady()  →  sense()  →  tracker->update()  →  act()  →  report()
  พร้อมไหม   GPS→ENU     อัลกอริทึมติดตาม     ส่งคำสั่ง   telemetry
```

`sense()` และ `act()` เป็นข้อตกลงกับ simulator จึงเหมือนกันทุกอัลกอริทึม
ส่วนที่เปลี่ยนมีเพียง `update()` เท่านั้น

### 10.4 วิธีที่ tracker เริ่มต้นกำหนดมุมเลี้ยว

แม้จะชื่อ Pure Pursuit แต่จริง ๆ เป็น**ผลรวมถ่วงน้ำหนักของ 5 พจน์** ค่าเริ่มต้นถูกตั้งให้
**พจน์ Pure Pursuit เป็นตัวนำ (น้ำหนัก 0.7) ส่วนที่เหลือทำหน้าที่ปรับละเอียดเท่านั้น**
Pure Pursuit แก้ทั้งค่าเบี่ยงเบนด้านข้างและความคลาดเคลื่อนของทิศพร้อมกันอยู่แล้ว การเพิ่ม
เกนของพจน์เสริมให้เท่ากันจึงเท่ากับแก้ความคลาดเคลื่อนเดียวกันสามครั้ง ทำให้พวงมาลัยค้าง
ที่ขีดจำกัดและเกิดอาการส่ายเป็นตัว S

| พจน์ | หน้าที่ |
|------|---------|
| Curvature feedforward | มุมเลี้ยวที่ความโค้งของเส้นทางเรียกร้อง (ไม่ขึ้นกับค่าความคลาดเคลื่อน) |
| Heading error (HDE) | ปรับทิศยานพาหนะให้ตรงกับเส้นสัมผัสของเส้นทาง |
| Lateral deviation (LTD) | ดึงยานพาหนะกลับเข้าเส้นทาง ในช่วงเลนวัดที่**ตำแหน่งคาดการณ์ล่วงหน้า 1.5 วินาที** ในช่วงเลี้ยววัดที่ตำแหน่งปัจจุบัน |
| Pure Pursuit | ส่วนโค้งไปยังจุดเป้าหมายข้างหน้า — **พจน์หลักของการเลี้ยว** (`ppWeightPp` ค่าเริ่มต้น **0.7**) |
| Yaw-rate damping | ลดการแกว่งเมื่อพจน์ข้างต้นตอบสนองมากเกินไป |

### 10.5 ★ คุณสมบัติที่ส่งผลต่อการออกแบบ RDDF โดยตรง

ส่วนนี้เป็นส่วนที่ใช้งานได้จริงที่สุดในหัวข้อนี้

| คุณสมบัติของ tracker | ให้เขียน RDDF แบบนี้ |
|----------------------|----------------------|
| ระยะมองล่วงหน้า = **ความเร็ว × 9 s** จำกัดที่ `5 m ~ 20 m` (ในช่วงโค้งจะสั้นลง) | โค้งที่รัศมีแคบกว่านั้นจะถูกตัดเข้าด้านใน ควรคลายมุมเลี้ยวที่หักมาก หรือวาง waypoint ให้ถี่ขึ้น |
| ประมาณความโค้งด้วยค่าเฉลี่ยถ่วงน้ำหนักจาก **5 จุดต่อเนื่อง** รอบจุดที่ใกล้ที่สุด | ระยะห่าง waypoint ที่ห่างเกินไปทำให้ประมาณความโค้งได้แย่ ส่วนโค้งจึงเลี้ยวไม่พอ (§4.4 กฎข้อ 4) |
| อุปกรณ์ต่อพ่วงอยู่**ด้านหลังยานพาหนะ 3 m** | การสลับ `implementFlag` เกิดตามเวลาที่อุปกรณ์ผ่านจุดนั้น ให้เผื่อระยะราว 3 m ที่ต้นและปลายเลน |
| ยังไม่เริ่มติดตามจนกว่าจะเข้ามา**ภายใน 3 m จาก waypoint ที่ 0** | จัด waypoint แรกให้ตรงกับจุด A ของแผนที่ (§4.4) |
| การสลับเดินหน้า↔ถอยหลัง**เบรกจนหยุดนิ่ง** | การเปลี่ยนทิศแต่ละครั้งเสียเวลาหยุดและเร่งใหม่ทั้งชุด ซึ่งถูกหักจาก `Time` โดยตรง การเลี้ยวแบบเดินหน้าจึงได้เปรียบ (§7.2) |
| เกณฑ์จบ = **ภายใน 1 m** จาก waypoint สุดท้าย **และ** อยู่ใน 10 waypoint สุดท้าย | ทำให้ waypoint สุดท้ายเป็นจุดสิ้นสุดที่ชัดเจน |
| ความคืบหน้าไม่ย้อนกลับ แม้เส้นทางจะตัดกันเอง | รูปแบบที่ข้ามเลนก็ปลอดภัย |

### 10.6 การสังเกตพฤติกรรมใน local

เมื่อรันแอปใน local จะมีผลลัพธ์วินิจฉัยสองอย่าง

- **`/tmp/pp_trace.csv`** — หนึ่งบรรทัดต่อหนึ่งรอบควบคุม: ตำแหน่งยานพาหนะ,
  มุมเลี้ยว, lateral deviation (LTD) และ**พจน์การเลี้ยวทั้ง 5 แยกกัน** ทำให้เห็นทันที
  ว่าพจน์ไหนพุ่งออกนอกลู่ หลังวิ่งเสร็จให้นำเส้นทางกับ trajectory จริงมาวางทับกัน
  ด้วย Python หรือสเปรดชีต
- **`PP_LOG ...`** (stderr) — สรุปหนึ่งบรรทัดต่อรอบ สำหรับดูสด ๆ ใน terminal

ถ้า `cte` สะสมไปข้างเดียวตลอดช่วงโค้ง แสดงว่าความโค้งของช่วงนั้นเกินขีดจำกัดทาง
กายภาพของแทรกเตอร์ ก่อนจะไปปรับ gain ให้ลอง**คลายความโค้งใน RDDF ก่อน**

### 10.7 ทดลองอัลกอริทึมอื่น

นอกจาก tracker เริ่มต้น ยังมีตัวอย่างการนำ Stanley ไปใช้มาให้ด้วย สลับได้ด้วย
ตัวแปรสภาพแวดล้อมตัวเดียว

```bash
PATH_TRACKER=stanley   # ค่าเริ่มต้นคือ pure_pursuit
```

การเพิ่มอัลกอริทึมของคุณเองมี 3 ขั้นตอน

1. สร้างคลาสใน `tracking/impl/` ที่สืบทอดจาก `PathTrackerBase` แล้ว implement
   `update()` และ `isTrackComplete()` (คัดลอก `StanleyTracker` เป็นจุดเริ่มต้นง่ายที่สุด)
2. เพิ่มหนึ่งบรรทัดใน registry ของ `TrackerFactory.cpp`
3. รันด้วย `PATH_TRACKER=<ชื่อ>`

`master_of_plow_app/tests/PathTrackerContractTest.cpp` ตรวจสอบ **tracker ที่ลงทะเบียน
ไว้ทุกตัว** โดยอัตโนมัติ ดังนั้นเมื่อทำขั้นที่ 2 เสร็จ การนำไปใช้ของคุณก็จะถูกทดสอบ
ด้วย ในสภาพแวดล้อมที่ติดตั้ง googletest ไว้ จะมีการ build target
`master_of_plow_app_tests` เพิ่มมาด้วย

การเปลี่ยนแปลงต้องผ่าน local tests และรวมอยู่ใน FIF ที่ได้รับอนุมัติ การแทนที่
binary โดยตรงไม่ใช่การรันอย่างเป็นทางการ

---

## 11. การขอความช่วยเหลือ

- **สอบถามเร่งด่วน**: ติดต่อทีมงานโดยตรงที่บูธในงาน

ขอให้โชคดี
